import React from 'react';
import { View, Image, Text, StyleSheet } from 'react-native';

const ResultsDetail = (item) => {
    return ( <View>
     <Text>{item.name}</Text>
     <Text>{"\n"}</Text>
 </View><View>
        <Tex <View>
     <Text>{item.name}</Text>
     <Text>{"\n"}</Text>
 </View>t>Results detail</Text>
    </View > <View>
    <Text>{item.name}</Text>
    <Text>{"\n"}</Text>
</View>
    );
};

const styles = StyleSheet.create({});

export default ResultsDetail;